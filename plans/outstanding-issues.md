BUG 2 — Legacy RMA field not removed/migrated RMA is still present in the output with value 'SS-2460627'. Spec §3.2.3 says: "Move RMA value into Project field." The Project field is present but contains 'HRC_Plasma_Sample_Handling_v2' (from StudyId), so the RMA value was not migrated and the legacy field was not dropped.

BUG 6 — MatrixTubeBarcode populated for array rows This is an NGS-only field per spec §2.6.2. Array rows in the output contain non-blank values like '0411046844'. In the source array ADAT these are Barcode2d values; the spec (§3.2.3) maps Barcode2d → MatrixTubeBarcode but classifies MatrixTubeBarcode as NGS-only. This is a semantic contradiction in the field mapping that needs resolution — if the value is kept for array rows it violates the NGS-only designation.